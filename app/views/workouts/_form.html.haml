= simple_form_for(@workout, html: {class: 'form-horizontal' }) do |f|
  = f.input :date, label: "Date"
  = f.input :mood, label: "How were you feeling?", input_html: { class: 'form-control' }
  = f.input :food, label: "How were you eating that day? (donuts again?)", input_html: { class: 'form-control' }
  = f.input :weather, label: "What was the weather like?", input_html: { class: 'form-control' }
  = f.input :wod, label: "WOD", input_html: { class: 'form-control' }
  %br/
  = f.button :submit